# Clave secreta para firmas de sesión Flask (requerida).
SECRET_KEY=<generar-con-openssl-rand-hex-32>

# Clave secreta para validar JWTs de Hydra IAM (requerida).
# Debe coincidir con la clave usada por Hydra Hub para firmar tokens.
JWT_SECRET=<valor-definir-por-administrador-hydra>
